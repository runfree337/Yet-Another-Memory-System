# Dashboard — current status

<!-- Template: kept SHORT and hand-maintained, updated at the CLOSURE of a work item
     (Definition of Done, backlog/README.md — the "status updated" step). This is neither the
     plan (= the milestone groups in `backlog/INDEX.md`, which order things) nor the todo
     (= `backlog/INDEX.md`, the work item detail) — only where things stand, to resume. The
     detailed, LIVE status view is GENERATED by the command below — never duplicated by hand
     here:

         python3 checks/backlog-check.py --board
-->

> Last updated: <YYYY-MM-DD> — <one-sentence gist of the last session>

## Progress by milestone

| Milestone | Progress | Next step |
|---|---|---|
| <Milestone N — milestone name> | <e.g. 2/5 work items built, or a status sentence> | <the next concrete action> |

## Hot spots

- <a live risk or blocker, in 1 line — what could derail things or is waiting on a decision>

<!-- A resolved hot spot gets deleted, not archived here (its history lives in
     git log / decisions/ if structural). Never let stale entries pile up. -->
